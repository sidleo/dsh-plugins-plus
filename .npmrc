# pnpm 11 默认在 run 前校验依赖树与 lockfile 一致性；
# 本项目 devDependencies 部分 @deepseek-ai/* 包（dsh-client-runtime 等）未发布到 npm
# （lockfile 里锁定的是本地/内网可解析的 rc 版本），校验会触发 install 拉取不存在的
# @deepseek-ai/dsh-compact / dsh-type-meta 等私有包导致 404。跳过该校验，直接用
# node_modules 里已装好的依赖构建。
verify-deps-before-run=false
